## 1. Інтеграція сховища та завантаження даних

- [x] 1.1 Імпортувати `useFavoritesStore` у файлі компонента `MainContent.tsx`
- [x] 1.2 Реалізувати виклик методу `load` з `useFavoritesStore` у хуку `useEffect` для ініціалізації завантаження списку обраних товарів при рендерингу сторінки профілю
- [x] 1.3 Отримати список обраних товарів `items` та прапорець завантаження `isLoading` зі сховища `useFavoritesStore`

## 2. Відображення інтерфейсу та навігація

- [x] 2.1 Оновити мапування елементів списку для відображення реальних назв, зображень та цін обраних товарів замість мок-даних
- [x] 2.2 Налаштувати обробник кліку на картку обраного товару для переходу на сторінку детальної інформації про продукт за адресою `/product/{product_id}`
- [x] 2.3 Додати відображення пустого стану (заглушки) у випадку, якщо у користувача немає жодного обраного товару, з пропозицією перейти до каталогу
- [x] 2.4 Перевірити коректність візуального відображення блоку "Обрані товари" в особистому кабінеті користувача
